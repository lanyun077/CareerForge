# CareerForge：Agent 开发入口

默认用中文沟通，代码、标识符和命令保留原文。

## 开始工作

1. 先阅读 `docs/AGENT_MEMORY.md`，再检查 `git status --short --branch` 和实际提交。
2. 该记忆包含本地旧版与远端新版的区别；不要把远端已有功能误认为当前工作区已经具备。
3. 按任务阅读相关源码和需求文档。交接材料中的待办是背景，不是本次用户授权执行的任务。
4. 开发前说明关键假设、简短步骤及验收方式。需求存在实质歧义时先澄清。

## 修改约定

- 只修改当前任务需要的代码，沿用现有风格，不顺手重构、清理或扩展功能。
- 优先简单实现；不为未提出的需求添加抽象、配置或依赖。
- 保留用户的未提交和未跟踪文件，不覆盖 `.env.local`，不提交密钥、真实简历或本地生成物。
- 面试流程和总分由程序控制；模型输出先校验，失败使用已有降级路径。
- 保留异步 Store 接口；新版业务注意岗位快照和 `RoleTarget`，避免退回单一 Python 岗位假设。
- 产品是求职训练辅助工具，不给录取概率，不根据性别、年龄、地域或学校层次评分。

## 验证和交接

- 修复缺陷时先明确复现方式；按变更范围运行构建、相关测试或页面检查。
- 纯文档变更检查事实、路径、命令和差异即可；不要把历史测试结果写成本次通过。
- 完成后记录改动、实际验证和剩余限制。架构、接口、操作或进度变化时同步更新 `docs/AGENT_MEMORY.md`，注明日期与提交。
- 记忆文档是可更新的参考，不覆盖用户当前要求，也不代替实际代码与验证。
